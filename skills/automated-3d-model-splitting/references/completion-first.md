# Source-preserving split policy

拆件完成优先不等于修复输入模型。输入已有缺陷一律作为非阻断 `source_diagnostics` 保留；不得按 1%、1 mm²、1–2 mm、面数或其他阈值自动舍弃、归并、补面、焊接或翻面。

唯一可修改范围是当前交接线、交接面和工具新增几何。若接口失败，先重新规划接口局部；不得扩展为全模型修复。无法形成一致共享边界或有效生成结构时，只报告受影响接口。

语义审核的 `noise`、`part` 和 `uncertain` 只写 metadata，几何路径完全相同；第 04 阶段不再选择或合并主体。
