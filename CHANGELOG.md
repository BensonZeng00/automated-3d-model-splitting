# 更新记录

## 未发布：默认装配差集（2026-09-12）

- 最终每对零件交叠合计小于对应实际切割体积的 1% 时静默忽略，替换原先固定 1 mm³ 阈值；分母使用原尺寸逐次布尔切割移除量，缩放和就位后保持不变，数值精度独立。
- 超出阈值且未解决的装配差异继续导出，提示用户观察判断打印影响，必要时自行调整。
- 默认开启最终缩放后的祖先实体差集，先消除干涉再检查就位；保留厚度、源表面、颜色及拓扑验收。
- 增加 `--no-post-fit-parent-difference` 显式关闭参数，同步 skill 流程和默认视觉验收约束。
- 默认保留未解决的装配干涉、遮挡和多视角问题，导出需手动调整的拆件；单件质量及颜色验收保持有效。
- 保存完整缩放后装配与薄背衬恢复输入，新增局部重放入口，装配异常保留实测详情。

## 未发布：2.1.0 发布准备修复（2026-09-10）

- 修复无依赖环境下预检提前崩溃和安装命令路径错误，兼容 PowerShell 与虚拟环境。
- 显式声明绘图依赖，增加无依赖预检与仓库发布文件回归测试。
- 忽略运行产物，发布校验覆盖整个仓库并输出 Skill 内容指纹。
- 统一默认源边界、可选平滑、长细条评审和微壳朝向规则。
- 保持当前 2.1.0 标识；本节尚未创建发布标签，验证状态见 TEST_RESULTS.md。

## 2.1.0 - 2026-09-07

- 完整同步本机 automated-3d-model-splitting 2.1.0，删除新版未包含的旧版实现、示例运行脚本及测试。
- 引入局部连接结构、打印容差、小零件确认、递归缓存和失败恢复。
- 采用完整子件精确相减和最终统一缩放装配。
- 同步参考文档、测试和诊断工具；仓库验证入口适配新版 CLI。
