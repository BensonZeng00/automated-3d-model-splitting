# 更新记录

本项目采用语义化版本号。

## 未发布

### 新增

- 增加可下载的 `cathead.3mf` 入门模型，以及原始模型和自动拆件结果示意图。
- 安装与预检成功后，先征得用户同意，再自动运行带 SHA-256 校验的猫头示例拆件测试。
- README 增加中文、英文、日文和韩文版本；默认入口继续使用简体中文。

## 1.3.5 - 2026-07-30

### 新增

- 严格按深度优先顺序执行递归拆件；每个子装配只接受父步骤刚生成并重新加载的独立彩色 3MF。
- 增加父体厚度射线测量、统一平面优先的自适应封底，以及嵌套零件封底决策复用。
- 增加运行时中文进度与机器可读 JSON 双通道日志，便于长任务监控和失败审计。
- 新增严格递归状态、运行时日志、父体厚度、材料槽和可视表面验证回归测试。

### 改进

- 默认使用 1.0 mm 有效最小深度和 5.0 mm 安全上限，并保留 0.05 mm 父体厚度余量。
- 小特征采用自适应装配间隙；嵌套叶子零件的有效间隙最高为 0.10 mm。
- 可视表面验证会剔除背面与掠射样本，减少隐藏封底和斜视栅格伪影造成的误报。
- 父体厚度候选查询按三角形半径分桶，在保持结果一致的同时减少无关相交测试。
- `--preflight-only` 现在可在尚未提供输入模型时独立检查 Python 依赖。

### 兼容性

- 保留 `scripts/split_painted_3mf.py` 作为稳定入口，非主体零件仍统一使用 `inward` 处理。
- 最终输出仍为一个顶层装配 build item，其中每个可打印零件对应一个网格对象。

## 1.2.0 - 2026-07-23

首个公开仓库版本，定位为“通用涂色 3MF 拆分”。

### 新增

- 从外部可见的逐三角形 `paint_color` 识别可打印颜色区域。
- 输出一个标准多零件装配 3MF，build 仅引用装配对象。
- Bambu Studio 材料槽顺序和项目材料配置保留。
- 装配结构、网格拓扑、法线、颜色、封闭性及可见表面的自动验证。
- 合成双色 3MF 端到端发布测试，以及版本和仓库卫生检查。

### 修复

- 防止输出退化成多个独立 build item 的非装配版本。
- 增加错误材料覆盖和生成表面侵入检查，防止封底遮挡嘴部、舌头等局部涂色区域。
- 对共享边界使用受约束整形，并保留角点和最大位移限制。

### 已知限制

- 当前只支持厂商逐三角形 `paint_color`，不支持仅使用标准 3MF `pid/pindex` 材料属性的输入。
- 歧义的多实例/多构建场景会明确拒绝，而不是猜测目标实例。
- 切片器视觉确认需要用户手动打开结果并提供截图。
