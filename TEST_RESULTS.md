# 2.1.0 入口定位与独立安装复查（2026-09-12）

对应当前仓库工作区，未提交或推送；本轮没有修改本机安装副本。以下历史记录仅代表各次检查时的状态。

`skill_sha256`: `22f699423fd919fba4bdba78a8bcbf0d8c459c0422552c1004b8a17e08190b6c`

- 环境：Windows / Python 3.11.15，共享 common-env，未安装或升级依赖。
- Skill 回归：406 项通过，30.803 秒。
- 发布工具测试：7 项通过，包括新增独立安装端到端测试。
- 独立安装：仅复制发布候选中的 170 个 Skill 文件到临时带空格目录，从另一个空项目目录调用绝对入口；无第三方依赖启动的帮助和版本通过，使用现有依赖的合成拆件通过，项目目录保持空白。
- 合成输出校验包含 2 个网格零件、1 个装配、1 个构建项，以及子件实际切割体积来源；复用同一个合成端到端校验器。
- 发布检查：193 个候选文件，零错误。新增根目录日期命名运行目录忽略规则；实验文件保留。
- Skill 格式检查通过；文档本地链接检查未发现缺失资源（排除说明中的路径占位符）。

入口文件在仓库技能目录和本机安装目录中均存在，仓库入口已受 Git 跟踪。当前项目根目录没有 `scripts/split_painted_3mf.py` 属于正常布局。Skill 已明确以实际加载的 `SKILL.md` 所在目录解析入口、依赖和工具，并说明安装时必须保留整个技能目录。

本轮没有执行干净环境依赖安装、真实模型重拆、切片器检查或实物试打。全量回归日志保存于忽略目录 `artifacts/entry-review-20260912/`。发布候选包含尚未跟踪的新模块和测试；正式提交需包含完整候选中的必要文件，不能仅提交已跟踪文件的修改。

---

# 2.1.0 发布准备验证（2026-09-12）

当前记录对应仓库工作区；未提交、推送或创建发布标签。本轮未同步本机安装副本，下方旧记录中的同步声明仅代表当时状态。

## 内容指纹与结果

`skill_sha256`: `b3410c7397cb9d17f2d53e7feea4b799f869eef928b570c81aa5cad0c34c6719`

Windows / Python 3.11.15，共享 common-env；没有安装或升级依赖。

- Skill 单元及回归测试：380 项通过（36.694 秒）。
- 发布工具测试：6 项通过。
- 发布内容检查：182 个候选文件，零错误；实验、重建和备份目录已忽略，原文件保留。
- Skill 格式检查：通过。
- 合成端到端：通过；768 个源面、2 个网格零件、1 个装配、1 个构建项。
- 合成输出 SHA-256：`8084cbac08ab54fc46f949766c1aed8752361a7907b50010af0472a89c7874b7`。
- 修复空格分隔零件列表漏选，新增逗号、空格、制表符、换行、混合分隔及空输入回归。
- 修复一个测试模块的导入路径，使其与其余测试使用同一包路径。
- 移除弯曲背衬修复示例的本机绝对路径，补充跨平台运行说明。

## 验证边界

本轮仅解析检查了工作流 YAML，尚未在远程运行。未执行干净环境安装、真实模型重拆、切片器检查或实物试打。

本轮完整回归日志保存在忽略目录 `artifacts/release-writeback-20260912/`。工作区既有六处文件末尾空行被 `git diff --check` 提示；本轮未修改这些无关文件。

---

# 历史：2.1.0 发布准备验证

验证日期：2026-09-10。对应当前工作区内容，尚未创建发布标签或推送。下方历史记录不代表此快照的当前状态。

## 本次内容指纹

`skill_sha256`: `0ccbf4af2bbb97133cbc74f282c80f6d73f640cc6fad47874afa3cd9f35f13ea`

指纹由 `tools/validate_release.py` 输出：按 Skill 相对路径排序，将每个路径、NUL、该文件的 SHA-256 十六进制值与换行依次送入 SHA-256。范围为发布候选 Skill 文件，排除运行缓存；它标识工作区文件的实际字节，不是 Git 提交号。

已同步本机安装副本：排除运行缓存后，仓库与安装版的 143 个文件逐文件 SHA-256 全部相同。本次同步更新 11 个文件（含新增文件），旧版已备份。

## 环境与结果

Windows，Python 3.11.15，共享 common-env。复用现有依赖，本次没有安装或升级共享环境中的软件包。

| 依赖 | 本次验证版本 |
| --- | --- |
| NumPy | 2.4.6 |
| SciPy | 1.17.1 |
| trimesh | 4.12.2 |
| networkx | 3.6.1 |
| manifold3d | 3.5.2 |
| matplotlib | 3.11.1 |
| Pillow | 12.3.0 |

- Skill 单元/回归测试：335 项全部通过。
- 仓库发布文件测试：6 项全部通过；覆盖 Git 忽略、被强行跟踪的运行产物、Skill 外的本机路径、公开模型允许清单和无 Git 的源码目录。
- 无第三方依赖启动：使用 Python `-S` 验证帮助、版本和预检；缺依赖时返回退出码 2 和安装提示，不再提前崩溃。
- 安装提示：验证指向真实 requirements.txt，去掉 `--user`，并实际执行 PowerShell 参数捕获脚本验证引号及空格路径。
- 发布一致性、依赖预检和 Skill 格式检查：通过。
- 合成端到端：通过；768 个源面，2 个网格零件、1 个装配、1 个构建项。输出 SHA-256 与本轮修复前相同。
- 平面弧边界与连接结构 harness：通过。
- 内向封底性能：2089 点生成 2087 面，0.235237 秒，预算 5 秒。

## 验证边界

本次未执行真实用户模型重拆、切片器人工检查或实物试打。无依赖启动测试不等于完整的干净环境安装测试；CI 已配置 Windows / Python 3.10、3.11、3.12 的依赖安装、依赖一致性检查及完整测试，尚未在远程执行，不能宣称这些矩阵版本已全部通过。

本次日志和安装版备份位于忽略目录 `artifacts/release-hardening-84t2tcnb/`，不进入发布包。

---

# 历史验证记录（仅代表各次验证当时的状态）

验证日期：2026-09-07。Windows，Python 3.11.15，共享 common-env。

- Skill 源文件与本机 2.1.0 逐文件 SHA-256 一致（排除运行缓存）。
- 单元和回归测试：296 项全部通过。
- 发布一致性与依赖预检：通过；预检使用临时合成 3MF。
- 合成端到端：通过，768 个源面，输出 2 个网格零件、1 个装配、1 个构建项。
- 平面弧边界 harness：通过。
- 连接结构 harness：全部用例通过。
- 内向封底性能：2089 点生成 2087 面，0.289 秒，预算 5 秒。

本次验证未执行真实模型拆件或切片器人工检查。

## 2026-09-08：打印优先优化（仓库工作区）

本节为上述 2.1.0 同步之后的代码变更；当时仓库 Skill 已不再与当时的本机安装副本逐文件相同；这不是当前同步状态。

- 完整单元/回归测试 302 项通过；随后新增缓存模式隔离检查，7 项专项测试全部通过。
- 连接件 harness 5 个用例、平面弧边界 harness、合成端到端、发布一致性检查通过。
- 2089 点内向封底性能检查：2087 面，0.255 秒，预算 5 秒。
- P05 保存现场局部对比：原严格细化 7.208 秒后仍超密度目标；生产保留路径 0.0065 秒，207 个隐藏面原样保留，新增顶点为 0。此为局部基准，不是整模型加速倍数。
- 猫头识别实际命中 1 个长细条评审候选（原 P01），可见面积 78.367 mm²、宽度估计 0.311 mm；已生成整体彩色定位/局部放大图，在切割前要求用户确认。
- 新筛选以可见源面为依据；2 面与 1200 面的同一细带都命中评审，不能因网格密度不同跳过确认。
- 新策略未重新生成猫头最终 3MF，也未进行实物试打。上一份猫头结果保持原样。

日志和局部基准位于 `artifacts/print-priority/`。

## 2026-09-09：源面对应与封底收敛优化（仓库工作区）

- 完整回归 309 项通过；最后收紧细分端点合成误差界后，6 项专项测试再次通过。
- 连接件 5 用例、平面弧边界、内向封底性能、合成端到端、发布一致性检查通过。
- 新增边界测试：0.02 mm 有序源环差异接受并保留输入坐标；错序/交叉源环、0.051 mm 偏差拒绝，严格零容错仍拒绝近似对应。
- 新增细分测试：面积大于微补片预算的有来源完整扇形，0.0005 mm 舍入误差通过且不执行最近表面搜索；重叠、缺少替换面、0.002 mm 偏差及严格模式均拒绝。
- 斜向封底局部对比：安装版测厚 8 次，优化版 2 次；安全上限 1.0 mm，最终最大深度分别 0.996085 mm / 0.99 mm。新方向复测变为不安全时仍拒绝输出。保留 24 次尝试上限、最小正深度和安全终止门槛。
- 数值证明、边界对应及回退策略模块进入实现缓存指纹，旧实现的阶段缓存不能冒充新结果。
- 未重拆猫头、未做实物试打、未同步本机安装副本。局部基准不代表整模型加速倍数。

日志和基准位于 `artifacts/numerical-print-policy/`。
